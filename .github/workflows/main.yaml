name: Elastic Beanstalk CI/CD
on:
  push:
    branches:
      - "master"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: "Print secrets"
        run: |
          echo ${{ env.DOCKERHUB_USERNAME }} | sed 's/./& /g'
          echo ${{ secrets.DOCKERHUB_TOKEN }} | sed 's/./& /g'
      - name: Checkout repository
        uses: actions/checkout@v4    
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          ecr: false
          # username: ${{ secrets.DOCKERHUB_USERNAME }}
          # password: ${{ secrets.DOCKERHUB_TOKEN }}
          username: "jainrounak6"
          password: "dckr_pat_hSmHLJzzYe-NJD9TTIfS5_MUUhA"
      - name: Build and push Docker images
        run: |
          docker-compose build
          docker-compose push
  deploy:
    runs-on: ubuntu-latest
    needs: build # Ensure the build job is completed before deploying

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-west-1

      - name: Deploy to Elastic Beanstalk
        run: |
          eb deploy Mern-sampleapp-env
